# .streamlit/secrets.toml.example
#
# DO NOT commit the real secrets.toml to git — it contains API keys.
# Copy this file to .streamlit/secrets.toml for local dev,
# OR paste these key=value pairs into the Streamlit Cloud secrets UI.
#
# In Streamlit Cloud:
#   App → Settings → Secrets → paste the block below (without the [secrets] header)

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_KEY = "your-supabase-anon-key"
ANTHROPIC_API_KEY = "your-anthropic-api-key"
ENVIRONMENT = "production"

# NOTE: TELEGRAM_BOT_TOKEN is NOT needed here — the dashboard never talks to Telegram.
# It lives only in Railway environment variables.
